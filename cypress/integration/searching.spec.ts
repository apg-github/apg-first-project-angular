import {app} from '../models/app.model';

describe('Searching tests', () => {
  beforeEach(() => cy.visit('/'));

  it('search and remove text from input', () => {

  });

  it('search for specific game', () => {

  });

  it('clicking home clears search input', () => {

  });
});
